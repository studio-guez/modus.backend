upload_max_filesize = 50M
post_max_size = 50M
memory_limit = 256M

; Development: recompile on every request so file edits are always picked up.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
